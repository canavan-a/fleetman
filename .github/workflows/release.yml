name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build binaries
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          # --- matches ClearBlade Edge support matrix ---
          - goos: linux
            goarch: amd64
            suffix: linux-amd64
          - goos: linux
            goarch: 386
            suffix: linux-386
          - goos: linux
            goarch: arm64
            suffix: linux-arm64
          - goos: linux
            goarch: arm
            goarm: "5"
            suffix: linux-armv5
          - goos: linux
            goarch: arm
            goarm: "6"
            suffix: linux-armv6
          - goos: linux
            goarch: arm
            goarm: "7"
            suffix: linux-armv7
          - goos: linux
            goarch: mips64
            suffix: linux-mips64
          - goos: linux
            goarch: mips64le
            suffix: linux-mips64le
          - goos: linux
            goarch: ppc64le
            suffix: linux-ppc64le
          - goos: linux
            goarch: s390x
            suffix: linux-s390x

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build agent
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
          CGO_ENABLED: "0"
        run: |
          go build \
            -ldflags "-X 'main.Version=${{ github.ref_name }}'" \
            -o dist/fleet-agent-${{ matrix.suffix }} \
            ./agent

      - name: Build server
        if: matrix.goarch == 'amd64'
        env:
          GOOS: linux
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: |
          go build \
            -o dist/fleetman-server-linux-amd64 \
            ./server

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.suffix }}
          path: dist/

  release:
    name: Create GitHub release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate install.sh
        run: |
          sed \
            -e 's|{{REPO}}|${{ github.repository }}|g' \
            -e 's|{{VERSION}}|${{ github.ref_name }}|g' \
            scripts/install.sh.tmpl > dist/install.sh
          chmod +x dist/install.sh
          cp scripts/uninstall.sh dist/uninstall.sh

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/fleet-agent-linux-amd64
            dist/fleet-agent-linux-386
            dist/fleet-agent-linux-arm64
            dist/fleet-agent-linux-armv5
            dist/fleet-agent-linux-armv6
            dist/fleet-agent-linux-armv7
            dist/fleet-agent-linux-mips64
            dist/fleet-agent-linux-mips64le
            dist/fleet-agent-linux-ppc64le
            dist/fleet-agent-linux-s390x
            dist/fleetman-server-linux-amd64
            dist/install.sh
            dist/uninstall.sh
          generate_release_notes: true
